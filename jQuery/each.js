/* El each es una funcion para que se aplique en modo de bucle
 a absolutamente todos los elementos que esta reconociendo. Ej:
 $("img").each(() => {
 como si fuera un bucle para cada elemento, la instancia se llamara
 This. Ej:
 $(this).fadeout(1000);

 De esta manera se podra aplicar algo para cada elemento que desees
 });
*/ 
